var CurriculumVitaeModel = function () {
    "use strict";

    //var self = this,
    //    personInformation = ko.observableArray();

    //_.each(personsInformation, function (personInfo) {
    //    personInformation.push(personInfo);
    //});

    //TODO: discuss assests.personInformation to be object not array of objects
}